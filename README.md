# Visual Tabs

Browser extension to configure the newtab page with random content. The data is
stored in the `data.json` file. You can edit the file to add more content or run
`deno task dev` to open a CMS ([Deno](https://deno.com/) is required).
